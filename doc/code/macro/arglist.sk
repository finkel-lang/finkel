;;;; File: arglist.sk

(defmodule Main
  (require (SK.Prelude)))

(eval-when (compile)
  (defmacro puts args
    `(putStrLn (unwords [,@args]))))

(defn (:: main (IO ()))
  (puts "foo" "bar" "buzz"))
