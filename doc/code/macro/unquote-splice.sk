;;;; File: unquote-splice.sk

(defmodule Main
  (require (SK.Prelude)))

(eval-when (compile)
  (defmacro uqs [arg]
    `(putStrLn (concat [,@arg]))))

(defn (:: main (IO ()))
  (do (uqs ("foo" "bar" "buzz"))
      (uqs ["foo" "bar" "buzz"])))
