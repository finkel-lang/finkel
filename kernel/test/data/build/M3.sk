(module M3 greet)

(import Language.SK)

(:: greet Macro)
(= greet
  (Macro (\ form
           (case (unCode form)
             (List [_ body]) (return `(putStrLn ,body))
             _ (skSrcError form "greet: malformed body")))))
