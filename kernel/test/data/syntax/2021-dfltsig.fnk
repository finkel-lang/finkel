%p(LANGUAGE DefaultSignatures)

(module Main)

(class (SPretty2 a)
  (:: sPpr2 (-> a String))
  (default (:: sPpr2 (=> (Show a) (-> a String))))
  (= sPpr2 show))

(instance (SPretty2 Bool))

(:: main (IO ()))
(= main (putStrLn (sPpr2 False)))
