;;; Setup script for SK package.

(require SK.Core)

(module SK.Setup
  defaultMainWithHooks
  skcHooks)

;; sk-kernel
(import Distribution.Simple.SK)

(defn (skcHooks UserHooks)
  ()
  (skcHooksWith "skc" False))
