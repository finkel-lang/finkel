;;; Setup script for SK package.

(require SK.Core)

(defmodule SK.Setup
  (use
   ;; sk-kernel
   (Distribution.Simple.SK))
  (export defaultMainWithHooks skcHooks))

(defn (skcHooks UserHooks)
  (skcHooksWith "sk" ["make"] False))
